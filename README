To setup the Docker image and environment, in the code-server-image folder run: 
./build.sh 

To create an environment for a specified user, run:
./create-codeserver.sh [USERNAME] [PORT]

This (re)generates a hashed password for the user, required when they navigate 
to "http://[SERVER_IP]:[PORT]"

